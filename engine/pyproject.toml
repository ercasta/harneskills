[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ugm"
version = "0.1.0"
description = "An entity-component world, a loop of rules over it, and one thread to run a session on"
readme = { text = "UGM", content-type = "text/markdown" }
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["ecs", "entity-component", "game-loop", "world-model",
            "forward-chaining", "arbitration"]

# No dependencies, and that is the whole of it. This is the substrate
# `harneskills` is built on -- a world, a loop, an engine to run one on --
# and it ships no domain, no channel and no transport, so nothing above
# it is baked in here.
#
# It ships a vocabulary (`facts.py`) and a way to arbitrate between
# rules that do not know about each other (`arbitration.py`) too -- a
# discipline rather than a domain: neither knows what a relation means, and
# both are what a rule has to do to compose with the ones it has never
# heard of. See the Scope section of README.md, and DECISION_PATTERNS.md.
#
# ⚠ ON HOLD as of 2026-08-29: `facts.py`/`arbitration.py`/`request.py` do
# not currently import against `world.py`'s own rewrite and are not
# imported by `ugm/__init__.py` -- see `docs/overview.md`.
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [".venv", "__pycache__", ".git", "*.egg-info"]

[tool.setuptools]
py-modules = []
packages = ["ugm"]
